services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: todo-api-laravel
    working_dir: /var/www
    volumes:
      - ./:/var/www
    ports:
      - "8000:8000"
    command: sh docker/start.sh
